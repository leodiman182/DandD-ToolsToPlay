const rollDice = require('../javascript/roll-dices')
const modalOff = require('../javascript/roll-dices')

describe('Test if the function RollDices', () => {
    it('has the type "function".', () => {    
        expect(typeof modalOff).toBe('function');
    });

    it('returns an element with the "object" type', () => {
     
    });

    it('uses the method "fetch".', () => {
      
    });

    it('uses the method "fetch" with the correct endpoint ("https://www.dnd5eapi.co/api/races/dragonborn")', async () => {
        
    });

    it(', when called with the parameter "races/dragonborn", returns the expected object', async () => {
       
    });
});